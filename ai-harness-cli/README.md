# AI Harness CLI

CLI tool for AI Harness Framework.

## Installation

```bash
npm install -g ai-harness-cli
```

## Commands

- `ah init` - Initialize harness in project
- `ah add <component>` - Add harness component
- `ah upgrade` - Upgrade harness components
- `ah validate` - Validate configuration
- `ah list` - List components
